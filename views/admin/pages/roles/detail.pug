extends ../../layouts/default.pug 
include ../../mixins/alert.pug

block main
    +alert-error(5000)
    +alert-success(5000)
    if (data.title)
        h1(class="mb-4") #{data.title}
    if(data.description)
        div(class="mb-4") 
            b nội dung mô tả:
            |   !{data.description}

    

